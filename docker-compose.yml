

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: personal-finance-backend
    restart: unless-stopped
    ports:
      - "7001:7001"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
